run the command: streamlit run code.py

The Result will show on the UI, after the terminal shows accuracy for part 3...
Part 3 takes sometime to load.
- before running, make sure to have all the libraries installed.
- Change the directory path of input files before running.